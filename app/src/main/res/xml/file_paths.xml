<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- For APK installation from external files dir -->
    <external-files-path
        name="downloads"
        path="Download/" />

    <!-- Fallback paths -->
    <external-path
        name="external_downloads"
        path="Download/" />

    <cache-path
        name="cache"
        path="/" />
</paths>
